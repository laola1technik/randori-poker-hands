import {expect} from "chai";
import "mocha";

describe.skip("Dealer", () => {
    describe("compares Hand", () => {
        describe("of Flush", () => {
            it("wins against high card", () => {
                // TODO: expect(dealer.compares(flush, highCard)).to.equal(Result.WIN);
            });
        });
    });
});
